import { StyleSheet } from 'react-native'

import { sizes } from '../../global-constants'

const buttonHeight = sizes.xxl

const { height } = StyleSheet.create({
  height: {
    height: buttonHeight,
  },
})

export const styles = {
  height,
}
